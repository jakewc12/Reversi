package DustinRaymondReversi.controller;

/**
 * Represents a controller for a game of Reversi. The controller will serve as a mediator between
 * the model and the players (human or AI) and ensures that the players play in order
 * and that the model's data is updated respectively.
 */
public interface ReversiController {
  // this interface is empty for now, since all the methods are abstracted into the
  // ReversiPlayerActions and ReversiModelFeatures. This interface serves as a place in case
  // extra functionality unique to the controller needs to be implemented.
}
